# Supabase project (not yet provisioned — see supabase/migrations for the schema
# these credentials will point to). Copy to .env.local once a project exists.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Paystack (wired in during the Cart & Checkout phase — unused for now)
PAYSTACK_SECRET_KEY=
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=

# Resend (transactional email — wired in during a later phase)
RESEND_API_KEY=
